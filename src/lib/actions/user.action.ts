"use server";

export async function signIn(params) {
  try {
  } catch (error) {
    console.error(error);
  }
}

export async function signUp(params: SignUpParams) {
  try {
  } catch (error) {
    console.error(error);
  }
}
